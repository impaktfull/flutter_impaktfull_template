import 'dart:ui';

class ThemeColors {
  ThemeColors._();

  static const primary = Color(0xFF1A1A1A);
  static const accent = Color(0xFF7D64F2);
  static const white = Color(0xFFFFFFFF);
  static const danger = Color(0xFFBA0F30);

  static const textPrimary = primary;
  static const textInvertedPrimary = white;
}
